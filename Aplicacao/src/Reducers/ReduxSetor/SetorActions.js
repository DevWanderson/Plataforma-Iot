export const SETOR = "SETOR";

export const setor = event =>({
    type: SETOR,
    payload: event
})

export const SELECT_SETOR = "SELECT_SETOR";

export const selectSetor = event =>({
    type: SELECT_SETOR,
    payload: event
})

export const DADOS_SETOR = "DADOS_SETOR";

export const dadosSetor = event =>({
    type: DADOS_SETOR,
    payload: event
})